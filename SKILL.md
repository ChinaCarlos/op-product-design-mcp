完整可安装 Skill 在 [`skills/spark-op-prototype/SKILL.md`](skills/spark-op-prototype/SKILL.md)。

规范、模板、CSS 随 MCP 包分发，生成时走 `get_brief` / `get_bundled_css`，不要按本文件的仓库相对路径去读。

**先安装 Node.js ≥ 18：** https://nodejs.org/zh-cn/download

两种安装方式都支持，选一种即可：

```bash
# 方式一
npx -y op-product-design-mcp install

# 方式二（类似 oh-my-zsh）
curl -fsSL https://raw.githubusercontent.com/ChinaCarlos/op-product-design-mcp/main/scripts/install.sh | bash
```
